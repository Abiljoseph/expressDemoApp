base url : http://localhost:3000/

Routing syntax
app.METHOD(PATH,HANDLER);
method is HTTP method (get, post, put, delete, etc).
PATH is the URL path to match
HANDLER is a callback that receives (req, res).
 